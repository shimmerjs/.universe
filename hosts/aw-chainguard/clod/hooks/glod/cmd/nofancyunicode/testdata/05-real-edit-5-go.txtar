# real Claude Edit.new_string from a transcript (main_test.go)
-- input.json --
{"tool_name":"Edit","tool_input":{"new_string":"// txtar fixtures live in testdata/*.txtar, populated from real Claude PreToolUse\n// payloads (each fixture's header comment records provenance) plus adversarial\n// cases. Each archive holds:"}}
-- want --
0
